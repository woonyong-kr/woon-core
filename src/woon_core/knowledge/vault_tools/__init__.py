"""Core-owned maintenance tools for an explicitly selected knowledge Vault.

These scripts operate on the process working directory so the Vault never owns
the executable source. The Woon CLI is responsible for selecting that directory.
"""
